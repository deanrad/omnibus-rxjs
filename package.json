{
  "name": "omnibus-rxjs",
  "version": "0.0.4",
  "license": "MIT",
  "author": "Dean Radcliffe",
  "repository": "https://github.com/deanrad/omnibus-rxjs",
  "main": "dist/omnibus-rxjs.min.js",
  "module": "dist/omnibus-rxjs.esm.js",
  "typings": "dist/tsc/types/src/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "npm run example:debounced",
    "example:debounced": "cd example/debounced-search; parcel index.html",
    "test": "yarn test:fast",
    "test:fast": "jest",
    "test:coverage": "jest --coverage",
    "stats:save:jest": "jest --coverage 1> docs/stats/coverage-report 2> docs/stats/spec-report",
    "stats:save:ast": "tsc; node scripts/code-stats.js > docs/stats/complexity",
    "stats:save:size": "size-limit > docs/stats/size",
    "stats:commit": "yarn stats:save:jest; yarn stats:save:ast; yarn stats:save:size; git add docs/stats; git commit -m 'Update code stats' -n",
    "test:quiet": "jest --silent --reporters=jest-silent-reporter",
    "test:full": "yarn test:fast && yarn test:quiet && echo 'Retest successful' && yarn build",
    "size": "size-limit",
    "doc": "typedoc",
    "doctoc": "doctoc .",
    "docs:commit": "yarn doc && git add docs/ && git commit -m 'Update docs' -n",
    "prepublishOnly": "yarn test:full && yarn stats:commit && yarn docs:commit",
    "build": "rimraf dist && tsc && rollup -c rollup.config.js && yarn size"
  },
  "size-limit": [
    {
      "path": "dist/omnibus-rxjs.esm.js",
      "limit": "10 kB"
    }
  ],
  "dependencies": {
    "rxjs": "^7.3.0",
    "tslib": "^2.3.1"
  },
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/preset-env": "^7.15.0",
    "@babel/preset-typescript": "^7.15.0",
    "@open-wc/building-rollup": "^1.10.0",
    "@phenomnomnominal/tsquery": "^4.1.1",
    "@rollup/plugin-typescript": "^8.2.5",
    "@size-limit/preset-small-lib": "^5.0.3",
    "@types/jest": "^27.0.1",
    "babel-jest": "^27.0.6",
    "deepmerge": "^4.2.2",
    "esprima": "^4.0.1",
    "esquery": "^1.4.0",
    "jest": "^27.0.6",
    "jest-silent-reporter": "^0.5.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.56.2",
    "size-limit": "^5.0.3",
    "typedoc": "^0.21.5",
    "typescript": "^4.3.5",
    "typescript-fsa": "^3.0.0"
  },
  "prettier": {
    "printWidth": 80,
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  }
}
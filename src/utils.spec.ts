describe('after', () => {
  describe('delay arg', () => {
    describe('when 0', () => {
      it.todo('is synchronous');
      it.todo('is faster than a Promise');
      it.todo('is faster than a setTimeout(fn,0)');
    });
  });
  describe('value arg', () => {
    describe('when value', () => {
      it.todo('is produced after the delay');
    });
    describe('when function', () => {
      it.todo('is produced');
    });
    describe('when function', () => {
      it.todo('is produced');
    });
    describe('when Observable', () => {
      it.todo('is produced');
    });
  });
});
